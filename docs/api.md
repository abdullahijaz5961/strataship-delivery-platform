# API

- `GET /api/releases` — list releases
- `POST /api/releases` — create a release record
- `POST /api/releases/{id}/promote` — promote to the next environment
- `POST /api/releases/{id}/rollback` — record a controlled rollback
- `GET /api/summary` — delivery metrics
