# StrataShip web interface

Dependency-free responsive dashboard built with semantic HTML, modern CSS, and browser JavaScript. It loads local fixture data immediately and connects to `http://localhost:8000/api` when the backend is available.

Run locally:

```bash
python -m http.server 5173
```
