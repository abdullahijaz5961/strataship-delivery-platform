# Contributing

1. Create a focused branch from `main`.
2. Keep commits small and descriptive.
3. Add or update tests when behavior changes.
4. Run `python -m pytest -q` from `backend/` and `node --check frontend/assets/app.js` before opening a pull request.
5. Explain the problem, approach, and verification steps in the pull request.
